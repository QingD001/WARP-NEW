# 预算内直接测量区域收益（2026-09-09）

本文取代旧方案中的 LightGBM 收益预测部分。模型实现、训练、交叉验证调用及运行依赖已删除。

## 已实现的流程

1. 只用 design/train queries 建立 Base 索引、分区、路由频率及区域特征；test 不参与选区。
2. 探测优先级为 `query_freq * gold_query_rate * (1 - local_base_recall) / estimated_cost`。
   local_base_recall 是区域内 gold 的召回，仅用于判断值得探测的机会，不冒充干预收益。
   第二次及之后每五次选择穿插一次固定种子的随机探索，缓解启发式漏选。
   每次从当前预算内可负担的区域中选择。
3. 默认探测区域数量最多约为 workload 覆盖区域的 20%（至少允许一个，不再强制六个）；
   累计估算建图成本不超过全图估算成本的 10%。不可负担时允许零探测，WARP 回退到 Base。
   每区最多均匀抽样 64 个设计问题，保存 eligible/sample 数量及逐题记录。
4. 用相同问题和最终部署的融合、重排、top-k 路径，测量 `Base+region` 相对 `Base` 的净变化。
   默认仍以整题 CompleteEvidence 为收益目标，EvidenceRecall 是已有可配置目标。
   收益允许为负；召回新增证据与丢失证据都进入评估，不能只计新增。
5. 对 n 个问题上的均值 g 做 `estimated_gain = n*g/(n+16)` 收缩。
   16 是可配置的正则强度，不是验证得到的最佳超参数。该估计不是置信下界或显著性检验。
   不同区域样本量相同则排序不会因收缩而改变，收缩也不能解决全零 CE 标签问题。
6. WARP 按 `query_freq * max(estimated_gain, 0) / estimated_cost` 贪心选区；Gain-only 按估计收益排序。
   两者只选已测正收益区域。未测区域报告为 unknown/unprobed，内部零值仅用于阻止选入。
   Random/Frequency/Cost 仍在所有区域上选择，不使用实测收益。
7. 复用探测图和检索结果，Reader 使用已保存的检索文档。如果没有区域图，Reader 在 full-graph
   baseline 提供后端后补评已保存结果，不重复检索。原始数据、逐题检索、LLM 调用和成本保存继续生效。

## 成本与公平性

10% 是建图 token proxy 上限，不是实际 LLM 输入输出 tokens 硬限额，也不覆盖探测在线检索。
实际构图与检索开销仍单独记录；需要严格美元/API 上限时还需在调用层增加额度与中断机制。
WARP 与 Gain-only 的 first-build 成本都包含探测构图和探测检索，并扣除部署图复用产生的重复项。
随机等无需收益探测的方法不应承担探测成本。当前对比是完整流程对比，不是固定候选池的纯排序消融。

探测预算低于部署预算时，WARP 可能提前饱和，即使部署预算还有余量也无法选择未探测图。
必须报告实际花费与选区数量，不能把曲线饱和解释为整个图集合已无收益。
区域过大时会被预算排除；下一步应评估预算感知的分区大小，而不是偷偷突破探测预算。

## 提升效果需要另外验证的假设

- CE 在现有 200 题上非常稀疏。先比较以 EvidenceRecall 和 CE 作为设计目标，最终统一报告 CE、ER、
  Reader EM/F1。仅在独立开发集上选目标与超参数，不用测试集选最好的设置。
- 单区域干预收益不等于多区域联合增益。跨区域补证存在互补与重排损失，本版本尚未实现条件边际收益
  选择；pair interaction 默认关闭以控制成本。下一阶段可针对少量候选集合实测条件收益并明确额外成本。
- 选区策略无法创造检索器本身没有的能力。先对比 Base 与全图上限，并检查图分支真实命中、dense fallback、
  路由遗漏、gold 是否进入候选及是否被重排丢弃，再决定是否投入多步检索。
- 多步检索属于另一个待实现/验证的干预，不能把本次选择器修改描述为已实现 IRCoT。应在同样检索步数、
  reader 和在线预算下比较 Base 与 WARP，记录每步新增 gold 与最终整题完成度。
- Random 多种子、逐题配对差异与置信区间仍必要。汇总指标相同不能推出结果列表相同。

## 验证边界

31 项单元与模拟集成测试通过，覆盖探测预算、少于六区域、零预算、正负收益收缩、未测区域报告，
以及既有检索/Reader/成本/快照/恢复逻辑。当前环境缺少真实 HippoRAG/FAISS 等运行条件，未进行付费
模型调用或完整数据实验。尚无证据证明本次修改提高真实检索指标。

# WARP-G: Workload-Aware Regional Provisioning for GraphRAG

## 摘要

GraphRAG 通过实体关系与图传播连接分散在多篇文档中的证据，但通常需要预先对语料执行知识抽取、向量化和图索引构建。现有高效方法主要降低单位构图成本，或依据语料结构选择核心内容；如何结合未来访问需求与图检索的实际收益分配构图预算，仍有待研究。本文提出 WARP-G（Workload-Aware Regional Provisioning for GraphRAG），将选择性构图建模为面向历史工作负载的区域物理设计问题。给定带有证据标注的设计查询，WARP-G 首先根据文档共访问关系划分语料，随后仅在少量区域上运行与部署一致的图增强检索路径，测量相对于基础检索器的边际收益，并利用构图前可得的特征预测未探测区域的收益。系统联合访问频率、预测收益与构建成本选择物化区域；在线阶段由覆盖全语料的混合检索器负责路由，仅调用相关且已物化的区域图。我们基于 HippoRAG2 实现该框架，并建立五折交叉拟合与等预算评测协议，在 HotpotQA、2WikiMultiHopQA、MuSiQue 和 PopQA 上考察证据检索、答案质量及设计、部署和在线成本。WARP-G 以少量真实探测支持区域图的价值估计，为有限预算下的 GraphRAG 部署提供工作负载感知的决策机制。

## 1. 引言

检索增强生成（Retrieval-Augmented Generation，RAG）通过引入外部知识，使语言模型能够访问可更新、可追溯的非参数化记忆 \citep{guu2020realm,lewis2020rag,gao2024ragsurvey}。典型 RAG 系统将文档切分为文本块，并利用 BM25 或 DPR 等检索器计算其与查询的相关性 \citep{robertson2009bm25,karpukhin2020dpr}。然而，独立排序文本块难以显式建模分散证据之间的联系，因而可能无法完整召回跨文档、多步推理所需的证据链 \citep{yang2018hotpotqa,xiong2021multihop}。

结构化索引为这一问题提供了补充途径。GraphRAG 将实体、关系和段落组织为图，通过社区摘要或图传播连接跨文档证据 \citep{edge2024graphrag,gutierrez2024hipporag,gutierrez2025hipporag2,guo2025lightrag}；面向具有明确篇章结构的文档，PT-RAG 保留学术论文的原生层次，并在上下文预算内沿相关路径组织证据 \citep{yu2026ptrag}。这些方法利用文本单元之间的联系改善证据组织，也使索引结构成为影响检索质量的重要因素。

更丰富的结构同时带来额外的离线投入。知识抽取、向量编码以及部分方法中的社区摘要需要在查询到达前完成，其成本随语料规模增长而累积。Wang 等在 EnterpriseRAG-Bench 上固定问题、相关证据与干扰文档，沿嵌套语料规模比较不同 RAG 范式，观察到 BM25 在较大规模下的竞争力，以及部分图方法受构建资源限制的现象 \citep{wang2026bm25scale}。这一规模研究使图索引的投入产出关系成为部署时必须考虑的问题：结构化检索的价值应由其相对强基础检索器的增益衡量，而构图范围应与可获得的收益相匹配。

现有高效 GraphRAG 主要沿两条路径降低投入。一类通过轻量化图表示或改进处理流程降低单位成本，如 E²GraphRAG、LinearRAG 和 ZoomRAG \citep{zhao2025e2graphrag,zhuang2026linearrag,hu2026zoomrag}；另一类缩减昂贵图结构的覆盖范围，如 KET-RAG 和 G2ConS 基于 PageRank 或概念中心性选择核心内容 \citep{page1999pagerank,huang2025ketrag,liu2025g2cons}。对于预算分配，语料结构之外还存在两个相关因素：不同区域承接的查询需求，以及图检索对这些查询的改善空间。高频区域可能已被基础检索充分覆盖，而结构中心性较低的区域也可能包含基础检索难以连接的关键证据。因此，仅凭访问量或中心性难以判断额外构图是否值得。

查询到达后的自适应检索能够进一步提高已有索引的利用效率。FLARE、Self-RAG 和 Adaptive-RAG 依据生成状态、反思机制或问题复杂度调整检索行为 \citep{jiang2023flare,asai2024selfrag,jeong2024adaptiverag}，EA-GraphRAG 在稠密检索、图检索及融合策略之间进行路由 \citep{dong2026eagraphrag}。VDGR-RAG 则在预建的层次异构图上结合目录路由、多路检索与动态反思，以支持企业知识问答 \citep{chen2026vdgrrag}。这类在线决策解决当前查询如何访问知识的问题；面向构建预算，还需要在未来查询到达前决定哪些区域图应被物化，并由后续查询重复使用。

这一问题与数据库中的索引和物化视图选择密切相关：系统需要根据历史工作负载、预期收益和资源预算选择物理结构 \citep{harinarayan1996datacubes,chaudhuri1997indexselection,agrawal2000viewsindexes,aouiche2017survey}。在 GraphRAG 中，这一决策面临额外的收益观测成本。区域的访问量和文本规模可以在构图前统计，但区域图能否弥补基础检索的不足，需要通过实际构图和检索才能测量。若对所有候选区域逐一构图以获得精确收益，便已付出希望避免的全量投入。由此，关键挑战是在有限探测成本下估计尚未构建区域的价值。

针对这一挑战，我们提出 WARP-G（Workload-Aware Regional Provisioning for GraphRAG），一种面向历史工作负载的区域图选择性物化框架。WARP-G 根据设计查询产生的文档共访问关系划分语料，在少量区域上执行真实图增强检索，利用证据标注测量相对于基础路径的增益，再通过区域特征预测其余区域的收益。预算选择器联合访问频率、预测收益和构建成本确定物化集合。新查询到达时，覆盖完整语料的基础检索器识别相关区域，系统仅调用其中已物化的区域图，并融合基础与图检索候选。

WARP-G 在既有图检索后端之上引入部署范围决策。本文以 HippoRAG2 为统一图后端，使受控预算选择方案共享 BM25、NV-Embed-v2、RRF 与 CrossEncoder 组件，并采用五折交叉拟合评价对未见查询的泛化。评测同时记录探测、部署与在线开销，以考察区域选择带来的质量—成本权衡。

本文的贡献如下：

1. 将 GraphRAG 索引部署形式化为面向工作负载的区域物理设计问题，在构建预算下优化未来查询的证据检索效用。
2. 提出查询共访问分区、少量真实探测与区域收益预测相结合的选择性物化流程，在无需构建全部候选区域图的条件下估计其部署价值。
3. 基于官方 HippoRAG2 实现区域构图、全语料路由与候选融合，联合需求、收益和成本选择区域，并按查询激活已物化图。
4. 建立交叉拟合与等预算评测协议，分别核算设计、部署和在线成本，通过固定后端与强基础检索对照检验物化策略。

## 2. 相关工作

### 2.1 图增强检索

图增强检索通过实体、关系、事实或层次结构连接文本块，通常涉及图索引、图引导检索和图增强生成等阶段 \citep{peng2024graphragsurvey,han2025graphragsurvey}。早期的 GRAFT-Net 和 PullNet 已利用问题相关子图支持多跳问答 \citep{sun2018graftnet,sun2019pullnet}。面向大语言模型，Microsoft GraphRAG 通过实体社区及摘要支持全局问答，HippoRAG 系列利用 Personalized PageRank 传播查询相关性，LightRAG 和 GRAG 则探索不同粒度的图结构与文本信息融合 \citep{edge2024graphrag,jeh2003personalizedpagerank,gutierrez2024hipporag,gutierrez2025hipporag2,guo2025lightrag,hu2025grag}。

层次化索引进一步利用文档单元之间的组织关系。RAPTOR 通过聚类和递归摘要形成多层检索结构 \citep{sarthi2024raptor}；PT-RAG 从学术论文的原生章节构建 PaperTree，并在 token 预算内选择相关路径，以保持检索上下文的连贯性 \citep{yu2026ptrag}。这些方法关注索引如何组织和呈现证据。WARP-G 将历史共访问关系用于形成可独立物化的区域，并以固定图后端上的实际增益判断区域的部署价值。

### 2.2 高效与选择性 GraphRAG

高效 GraphRAG 通过简化实体抽取、图表示或检索过程降低系统成本，包括轻量摘要树与实体图、基于依存分析的企业方案、无关系 Tri-Graph 和多尺度图检索 \citep{zhao2025e2graphrag,min2025practicalgraphrag,zhuang2026linearrag,hu2026zoomrag}。社区层次及摘要流程也可通过 k-core 分解和 token 感知采样提高效率 \citep{hossain2026coregraphrag}。随着语料增长，构建成本与检索质量需要联合评价；Wang 等的规模研究通过统一计量离线构建和在线查询投入，揭示了不同检索范式的质量—成本关系随规模变化的现象 \citep{wang2026bm25scale}。

选择性构图进一步减少昂贵索引的覆盖范围。KET-RAG 和 G2ConS 基于 PageRank 或概念中心性选择核心文本 \citep{page1999pagerank,huang2025ketrag,liu2025g2cons}。WARP-G 同样研究有限范围的图部署，但将选择依据扩展为历史需求与相对基础检索的边际收益。由于未构建区域的收益不可直接观测，WARP-G 通过少量真实探测学习收益估计，再联合构建成本进行预算分配。

### 2.3 自适应检索与查询路由

GraphRAG 的收益会随任务和语料条件变化 \citep{xiang2026whengraphs}。自适应 RAG 因此通过生成置信度、反思标记或问题复杂度决定是否检索及检索深度 \citep{jiang2023flare,asai2024selfrag,jeong2024adaptiverag}，并进一步探索检索策略路由及质量与资源消耗之间的权衡 \citep{dong2026eagraphrag,wang2026ragrouterbench,qian2026activerag}。在企业知识问答中，VDGR-RAG 预先构建融合目录、文本块和实体的层次异构图，再通过领域路由、多路检索、目录回溯和动态反思协调证据搜索 \citep{chen2026vdgrrag}。

上述方法根据当前查询决定访问哪些已有结构以及投入多少在线计算。WARP-G 的决策发生在物化阶段：它利用历史查询估计未来需求，在构建预算内决定哪些区域图可供后续访问。在线路由与离线物化可以共同作用于系统效率；本文固定在线检索路径，以考察部署范围选择的贡献。

### 2.4 工作负载感知的物理设计

WARP-G 在方法上与数据库物理设计密切相关。经典研究依据工作负载、收益与预算选择数据立方体视图、索引及物化视图 \citep{harinarayan1996datacubes,chaudhuri1997indexselection,agrawal2000viewsindexes,aouiche2017survey}；后续工作进一步学习数据分区、估计物化收益，或在空间预算下选择可复用中间结果和图查询视图 \citep{sheng2023wisk,han2023gnnmv,ciaperoni2022junctiontrees,pang2024rpqviews}。工作负载感知的知识图谱分区也根据查询模式组织 RDF 分片，以减少跨分区连接 \citep{priyadarshi2021wawpart,priyadarshi2021awapart}。这些研究为以需求和收益指导结构部署提供了基础。WARP-G 将这一思路用于从原始文本构建图索引的场景，以查询共访问关系形成物化单元，并通过少量实际构图观测学习其检索收益。

## 3. 方法

WARP-G 研究如何将有限的构图预算分配给最有价值的语料区域。这里的价值同时取决于三个因素：区域承接多少查询、图检索能为这些查询带来多少额外收益，以及构建该区域图需要多少成本。**核心挑战在于，前两者中的图检索收益必须经过真实构图才能观测。** 若先构建所有区域再比较收益，选择性部署就失去了节省构建投入的意义。

为此，WARP-G 将物理设计组织为一个闭环：利用历史查询形成区域，在少量区域上真实构图并测量增益，以构图前特征预测其余区域的收益，再联合需求与成本选择物化集合。在线阶段，完整语料上的基础检索负责路由，仅调用相关且已物化的区域图。正文说明各环节的设计依据与衔接，具体计算、参数和执行流程见附录 A。

### 3.1 问题定义与整体框架

给定文档语料 \(D=\{d_1,\ldots,d_m\}\)、带有证据标注的历史设计工作负载 \(Q_{\mathrm{des}}\)、基础检索器 \(H\)、固定图后端 \(G\) 和构建预算 \(B\)，系统首先形成区域集合

\[
\mathcal R=\{r_1,\ldots,r_n\},\qquad
r_i\cap r_j=\varnothing\;(i\ne j),\qquad
\bigcup_i r_i=D.
\]

每个区域是可独立构建、选择和复用的物化单元，其估算构建成本为 \(c_i\)。目标是在预算内选择集合 \(S\subseteq\mathcal R\)，最大化未来同分布查询上的期望检索效用：

\[
S^*=\arg\max_{S\subseteq\mathcal R,\;\sum_{r_i\in S}c_i\le B}
\mathbb E_{q\sim Q_{\mathrm{future}}}
\left[M(q;H,G_S)\right].
\]

其中，\(G_S\) 表示已物化区域图的集合，\(M\) 主要采用 Complete Evidence@10，即前十个结果是否包含回答问题所需的全部标注证据。该目标强调完整证据链，因为仅增加部分相关文档未必足以支持多步问答。

基础检索器 \(H\) 始终覆盖完整语料，并为区域划分、设计查询关联和在线路由提供统一入口；最终结果均经过共享的融合与重排流程。预算 \(B\) 约束最终部署的区域图，设计阶段的探测开销单独核算。当 \(B=0\) 时，系统退化为相同的无图基础路径。设计查询及其证据标注用于离线决策，测试查询不参与分区、特征计算、探测、训练或选择。

### 3.2 以查询共访问形成物化单元

**区域划分的目的，是把工作负载中经常共同访问的文档组织为同一个图部署单元。** 对每个设计查询，WARP-G 取得基础检索的前 \(k_c\) 个文档，并以文档对在同一结果列表中共同出现的次数建立共访问边：

\[
w_{\mathrm{qry}}(u,v)=
\sum_{q\in Q_{\mathrm{des}}}
\mathbb I[d_u,d_v\in\operatorname{TopK}_{H}(q)].
\]

这一关系反映当前工作负载下的候选访问模式，使共同服务于查询的文档更可能进入同一区域。它并不保证相连文档都是正确证据，也会包含基础检索的歧义；后续收益探测用于判断由这些候选形成的区域是否值得构图。

历史查询无法覆盖全部语料，因此系统补充少量低权重的语义近邻边，为缺少共访问记录的文档提供连接。在该稀疏加权图上运行 Leiden 社区发现，并合并过小社区，得到覆盖全语料的区域集合 \citep{traag2019leiden}。查询边决定主要结构，语义边承担平滑作用。

这里的文档共访问图只用于确定区域边界，发生在昂贵的知识抽取之前。后续在区域内部构建的 HippoRAG2 知识图才参与图检索。这样的先后关系使系统能够在尚未支付全量构图成本时，获得一组可供选择的物理结构。边权组合与划分参数见附录 A.1。

### 3.3 构图前刻画需求与改善空间

区域被频繁访问，只说明它承接较多需求；若基础检索已能完整召回证据，额外构图的价值仍可能很低。WARP-G 因此同时刻画区域的访问需求与基础检索的改善空间。

设 \(Q_i\) 为基础路由命中区域 \(r_i\) 的设计查询集，即查询的前 \(k_r\) 个基础结果中至少有一篇文档属于该区域。访问频率定义为 \(F_i=|Q_i|\)。同一查询可以关联多个区域，但在同一区域内只计一次；这与在线阶段允许调用多个区域图的机制一致。

每个区域的特征向量 \(\mathbf x_i\) 包含三组信号。第一组是文档数、文本规模和访问频率，用于描述成本与需求；第二组是基础证据召回、完整证据失败率和候选分数熵，用于反映基础路径的不足与不确定性；第三组是多文档证据需求、语义离散度和内部共访问密度，用于描述可能影响图检索价值的区域结构。

所有特征均在区域知识图构建前计算。其中，证据召回、失败率和多文档需求使用设计集标注，因此收益建模属于有监督的物理设计。**这些特征用于预测图能否改善检索，真正的收益标签仍由下一阶段的实际检索对照产生。** 九维特征的定义与聚合口径见附录 A.2。

### 3.4 用少量真实探测学习区域收益

WARP-G 从具有设计查询覆盖的区域中，依据访问密度和基础检索失败率选取探测集合 \(P\)，以覆盖不同需求与检索难度的区域。对每个 \(r_i\in P\)，系统使用与部署相同的图后端建立独立索引，并对 \(Q_i\) 中的查询比较两条路径：基础候选经过融合与重排得到结果；在相同基础候选中加入该区域图候选，再使用同样的融合与重排得到增强结果。

区域收益定义为

\[
g_i=\frac{1}{|Q_i|}\sum_{q\in Q_i}
\left[M(q;H,G_i)-M(q;H)\right].
\]

这一对照衡量的是区域图对最终检索结果的边际贡献。即使图检索找到相关文档，若这些文档已被基础路径覆盖，或未能通过最终重排，标签也不会自动增加。反之，若图候选干扰了排序，收益可以为负。主标签使用 Complete Evidence@10，同时记录 Evidence Recall@10 的变化，以区分完整证据补全与部分证据改善。

随后，以探测样本 \(\{(\mathbf x_i,g_i):r_i\in P\}\) 训练 LightGBM 回归器 \(f_\theta\) \citep{ke2017lightgbm}，并为区域赋予收益估计

\[
\hat g_i=
\begin{cases}
g_i, & r_i\in P,\\
f_\theta(\mathbf x_i), & r_i\notin P.
\end{cases}
\]

已探测区域保留实测值，未探测区域依靠特征外推，从而将获取收益标签的真实构图投入限制在少量区域内。被选中的探测图可以直接复用。预测质量通过区域级留一验证、特征消融和探测样本学习曲线检验；这些分析衡量收益是否可预测，不预设少量探测必然足够。采样与训练细节见附录 A.3。

### 3.5 联合需求、收益和成本分配预算

\(g_i\) 是条件于访问区域 \(r_i\) 的平均收益。要衡量其对整体工作负载的价值，还需要考虑该区域被访问的频率。在区域收益可近似相加的条件下，整体平均增益可用

\[
\Delta(S)\approx\frac{1}{|Q_{\mathrm{des}}|}
\sum_{r_i\in S}F_i\hat g_i
\]

估计。由于归一化因子对所有候选集合相同，WARP-G 使用以下单位成本价值进行排序：

\[
s_i=\frac{F_i\cdot\max(\hat g_i,0)}{c_i}.
\]

**这一选择规则把“访问多少次”“每次能改善多少”和“为此需要花费多少”连接起来。** 因而，高频但基础检索已足够好的区域未必优先；平均收益较高但很少被访问、或构图成本过大的区域也未必最划算。\(c_i\) 使用构图前可得的文本规模代理，实际成本在构建后另行测量。

系统按 \(s_i\) 降序遍历区域，选择收益为正且能放入剩余预算的区域；遇到过大区域时跳过并继续检查后续候选。该贪心规则提供可直接执行的预算方案，但不是原始期望效用目标的精确求解。同一查询可能调用多个区域，候选融合会产生互补或竞争，因此本文额外测量区域对的二阶交互，以检验独立收益近似。选择流程、对照规则与交互定义见附录 A.4。

### 3.6 区域物化与在线检索

每个选中区域对应一个独立的 HippoRAG2 索引。区域之间共享模型权重，图节点、向量库及构建产物保持隔离，使区域能够独立选择、计费和复用。系统复用选中的探测图，并补建其余选中区域，最终仅将集合 \(S\) 中的图用于部署检索。

新查询首先在完整语料上执行混合检索，根据前 \(k_r\) 个文档的区域归属形成 \(A(q)\)。随后仅调用同时满足相关性路由与物化条件的区域，即 \(S\cap A(q)\)，并统一融合、重排候选：

\[
\operatorname{Result}(q)=\operatorname{Rerank}
\left(\operatorname{RRF}
\left[H(q),\{G_i(q):r_i\in S\cap A(q)\}\right]\right).
\]

基础路径保证未构图区域仍可被检索，区域图为选中的部分补充结构化传播能力；当没有命中已物化图时，系统使用相同的基础融合与重排路径。这样，离线物化决定哪些图值得长期保留，在线路由决定当前查询实际访问哪些图。

该机制也界定了方法的能力边界：未被基础路由命中的区域无法提供图增强，不同区域图之间不进行连续图传播。因此，评测同时检查正确证据区域的路由覆盖，并与保留跨区域连接的全语料单图比较。共享检索组件与具体执行深度见附录 A.5。

### 3.7 训练与评测原则

本文采用五折交叉拟合，每折重新使用设计查询完成区域划分、特征统计、探测、收益预测与预算选择，仅在留出查询上评价最终方案。受控区域选择实验共享分区、成本代理、图后端及在线检索配置，以比较联合选择和不同单因素规则的效果；无图基础路径和全语料完整图分别提供基础能力与完整图部署的参照。

评测同时考察证据检索、固定读者模型下的答案质量，以及设计、部署和在线三个阶段的投入。尤其是，少量探测本身也需要真实构图，不能仅凭最终物化集合的大小判断方法是否节省成本。预算设置、指标、消融与成本核算口径见附录 A.6。

## 参考文献

正文采用 Natbib 风格引用，BibTeX 条目见 [references.bib](references.bib)。

## 附录 A. 方法实现与评测细节

### A.1 共访问图与区域划分

划分图以语料中的文档单元（实现中的 passage）为节点。基础检索器使用 BM25 与 NV-Embed-v2，先通过 RRF 合并两路结果；此处尚未使用 CrossEncoder。对每个设计查询取前 \(k_c=20\) 个文档，将其中不同文档两两连接，每共同出现一次，边权增加 1。所有查询的贡献累加为正文中的 \(w_{\mathrm{qry}}\)。区域边界不直接使用正确证据标注。

在共享稠密表示空间中，为每个文档搜索 \(k_s=5\) 个余弦近邻，仅保留相似度为正的近邻边。同一无向文档对若被两个方向同时找到，保留最大相似度而不重复累加。最终边权为

\[
w(u,v)=w_{\mathrm{qry}}(u,v)+\lambda w_{\mathrm{sem}}(u,v),
\qquad \lambda=0.05,
\]

其中非近邻文档对的 \(w_{\mathrm{sem}}\) 为零。一次查询共现贡献 1，单条语义边贡献至多约 0.05，因而语义连接只作为较弱的补充。图中保留全部文档节点，包括没有查询共访问边的文档。

在加权无向图上，使用 Leiden 的 `RBConfigurationVertexPartition`，分辨率为 1.0，随机种子为 42。对少于 5 篇文档的社区，计算其与大小合格社区之间的总连接权重，并优先并入连接最强者；若没有这样的连接，则确定性地并入编号最小的其他非空社区。合并后按文档标识排序并生成稳定的区域编号。区域数量由图结构与分辨率决定，不预先指定。

### A.2 查询关联与九维区域特征

特征计算先为每个设计查询取得 50 个混合检索候选，并使用其中前 \(k_r=20\) 个形成路由列表 \(L_q\)。区域关联查询集为

\[
Q_i=\{q\in Q_{\mathrm{des}}:L_q\cap r_i\ne\varnothing\}.
\]

以 \(E_q\) 表示查询的正确证据文档集合，\(e_d\) 表示文档的稠密向量。特征按以下顺序组成向量：

\[
\mathbf x_i=[N_i,T_i,F_i,R_i,E_i,H_i,U_i,V_i,C_i].
\]

| 特征 | 定义与统计范围 |
|---|---|
| \(N_i\)：文档数 | 区域内文档数量 \(\lvert r_i\rvert\)。 |
| \(T_i\)：文本规模 | 区域内文本经统一确定性分词后的 token 数之和。 |
| \(F_i\)：访问频率 | 关联查询数 \(\lvert Q_i\rvert\)，同一区域内按查询去重。 |
| \(R_i\)：基础证据召回 | 对 \(Q_i\) 中有证据标注的查询，平均 \(\lvert E_q\cap L_q\rvert/\lvert E_q\rvert\)。 |
| \(E_i\)：完整证据失败率 | 对相同查询，平均 \(\mathbb I[E_q\nsubseteq L_q]\)。 |
| \(H_i\)：检索分数熵 | 对路由列表中属于该区域的候选，将非负检索分数归一化后计算归一化熵，再对有效查询取平均。 |
| \(U_i\)：多文档需求 | 在 \(Q_i\) 中满足 \(E_q\cap r_i\ne\varnothing\) 的查询上，统计 \(\lvert E_q\rvert>1\) 的比例。 |
| \(V_i\)：语义离散度 | 区域内文档对的平均余弦距离 \(1-\cos(e_u,e_v)\)。 |
| \(C_i\)：共访问密度 | 区域内部查询边总权重，除以可能文档对数与设计查询数的乘积。 |

这里的 \(R_i\) 和 \(E_i\) 评价整题证据在基础路由列表中的覆盖情况，不仅评价区域内部证据，也不等同于重排后的探测指标。\(U_i\) 则只在区域含有该题正确证据时计入该查询，但判断的是整题是否需要多篇文档。

具体而言，对查询 \(q\) 在区域中的候选分数 \(a_1,\ldots,a_\ell\)，取 \(a_j=\max(\operatorname{score}_j,0)\)，当总分为正且 \(\ell>1\) 时计算

\[
p_j=\frac{a_j}{\sum_t a_t},\qquad
h_{qi}=-\frac{\sum_{j=1}^{\ell}p_j\log(p_j+\epsilon)}{\log\ell},
\qquad \epsilon=10^{-12}.
\]

\(H_i\) 为有效 \(h_{qi}\) 的平均值。离散度 \(V_i\) 在文档对数不超过 4096 时枚举全部文档对，否则以固定种子及区域编号确定性地采样 4096 个不同文档对。共访问密度只使用查询边，不计入语义边：

\[
C_i=\frac{\sum_{u<v,\;d_u,d_v\in r_i}w_{\mathrm{qry}}(u,v)}
{\max\{\binom{N_i}{2}|Q_{\mathrm{des}}|,1\}}.
\]

不存在有效观测的均值特征置为零。\(T_i\) 使用与成本代理一致的分词规则：文本转小写后匹配连续 Unicode 单词字符；这一计数用于区域间的统一比较，不等同于图后端实际计费的模型 token 数。全部特征均不依赖已构建区域图的节点、边或图检索统计。

### A.3 探测样本、收益标签与预测器

探测候选仅包含 \(F_i>0\) 的区域。设这类区域数为 \(n_+\)，探测数量为

\[
n_P=\min\{n_+,\max(6,\operatorname{round}(\rho n_+))\},
\qquad \rho=0.2.
\]

当前流程要求至少有六个工作负载覆盖区域，以支持后续区域级验证。具体采样时，按 \((F_i/T_i,E_i,\operatorname{id}_i)\) 依次排序，将排序序列按索引对 \(n_P\) 取模分入 \(n_P\) 个桶，再用固定种子从每桶抽取一个区域。这是基于排序与交错分桶的采样，失败率作为访问密度相同时的次级排序依据。

每个探测区域独立构图，并复用 A.2 中缓存的 50 个基础候选。对其每个关联查询，执行以下对照：

1. 基础路径仅输入基础候选，经统一 RRF 与 CrossEncoder 取得前 10 个结果。
2. 图增强路径从该区域图取得候选，与同一基础候选融合，保留 50 个候选供相同 CrossEncoder 重排，再取得前 10 个结果。
3. 分别计算两条路径的完整证据命中与证据召回，求逐题差值并在 \(Q_i\) 上取平均。

对最终前 \(k\) 个文档集合 \(L_q^{(k)}\)，两项指标定义为

\[
\operatorname{CE}@k(q)=\mathbb I[E_q\subseteq L_q^{(k)}],\qquad
\operatorname{ER}@k(q)=\frac{|E_q\cap L_q^{(k)}|}{|E_q|}.
\]

探测查询必须具有非空证据标注。训练标签采用 \(\operatorname{CE}@10\) 的平均差值，\(\operatorname{ER}@10\) 的变化作为辅助记录。负标签保留在训练集中，不在训练前截断；仅在区域选择评分中将负收益截为零。

LightGBM 使用 100 棵树、学习率 0.05、最多 7 个叶节点、叶节点最少 2 个样本，随机种子为 42。训练仅使用探测区域的九维特征与实测标签。模型预测全部区域后，以实测值覆盖探测区域的预测值。

收益预测验证采用逐区域留一：每次移除一个探测区域的标签，用其余区域训练并预测该区域，汇总 MAE、RMSE 和 Spearman 相关系数。特征消融逐项将特征置零后重复验证。学习曲线在已获得的探测标签内抽取不同规模的训练区域，在剩余区域上评价，每个规模重复 20 次；它衡量标签利用效率，不额外构建未探测图。

### A.4 成本代理、预算选择与区域交互

区域成本代理为 \(c_i=T_i\)，选择器要求成本为正，相对预算 \(b\) 转换为 \(B=b\sum_i c_i\)。这一预算约束不可拆分区域的累计文本规模，实际 LLM 与嵌入成本在构图后记录。WARP-G 的具体选择过程为：

1. 对全部区域计算 \(s_i=F_i\max(\hat g_i,0)/c_i\)。
2. 按得分降序排序；同分时优先成本较小者，再按区域编号确定顺序。
3. 初始化物化集合为空、剩余预算为 \(B\)。依次跳过 \(\hat g_i\le0\) 的区域，将能放入剩余预算的区域加入集合并扣除其成本；对放不下的区域继续跳过。
4. 复用集合中的探测图，构建其余选中区域，并将最终集合用于在线检索。

探测图的存在不代表该图必然被部署。预算也不要求耗尽：即使 \(b=1\)，收益非正的区域仍会被跳过，因此 WARP-G 的最大预算点不等同于 Full Graph。

受控选择对照在相同区域和成本代理上分别采用随机顺序、访问频率降序、收益降序与成本升序。Gain-only 同样跳过非正收益区域；其他单因素规则按自身顺序填充预算。这些对照用于检验联合需求、收益和成本的作用。

为诊断独立收益近似，从探测区域中最多抽取 30 对。对每一对 \((i,j)\)，在共同的查询集合 \(Q_i\cup Q_j\) 上，分别测量基础路径、仅允许区域 \(i\)、仅允许区域 \(j\) 和同时允许两区域时的平均效用 \(U_{ij}(S)\)。四种设置均执行实际路由与相同候选排序，定义

\[
I_{ij}=U_{ij}(\{i,j\})-U_{ij}(\{i\})-U_{ij}(\{j\})+U_{ij}(\varnothing).
\]

正值表示联合收益超过独立增益之和，负值表示存在冗余或竞争。所有项在同一查询集合上计算，避免混用不同区域平均值。交互项仅用于诊断，不进入当前选择评分；相应检索调用开销单独记录。

### A.5 区域索引与在线执行配置

区域图使用官方 HippoRAG2 后端。每个区域维护独立索引产物，共享模型权重；选中的探测索引直接复用。图后端在各区域内部执行其知识抽取、索引与图传播流程，区域之间通过结果融合共同服务查询，不连接成跨区域传播图。

在线先取得 50 个混合候选，以前 20 个文档所属区域确定路由集合，按区域去重后与物化集合取交集。每个被调用区域提供最多 50 个候选；基础列表与区域图列表通过 RRF 融合，保留 50 个候选供 CrossEncoder 重排，输出前 10 个结果。基础路径不受已选区域的文档范围限制。

RRF 对候选列表按排名而非原始分数融合 \citep{cormack2009rrf}：

\[
\operatorname{RRF}(d)=\sum_{L:\,d\in L}\frac{1}{60+\operatorname{rank}_L(d)}.
\]

各列表使用相同权重，文档按标识合并，同分时按文档标识确定顺序。混合检索内部先融合 BM25 与 Dense；最终融合将所得基础列表与各区域图列表作为输入。所有基础、探测和部署路径共用最终排序函数。

四个数据集的正式配置共享以下主要设置：

| 项目 | 配置 |
|---|---|
| 共访问深度 / 语义近邻数 / 语义权重 | 20 / 5 / 0.05 |
| Leiden 分辨率 / 小社区合并阈值 | 1.0 / 5 篇文档 |
| 探测比例 / 随机种子 | 0.2 / 42 |
| 路由深度 / 融合候选深度 / 输出深度 | 20 / 50 / 10 |
| 基础与图后端嵌入模型 | `nvidia/NV-Embed-v2` |
| 图抽取模型 | `gpt-4o-mini-2024-07-18` |
| 图后端检索深度 / 链接深度 | 200 / 5 |
| 图传播阻尼 / 段落节点权重 | 0.5 / 0.05 |
| 同义边近邻上限 / 相似度阈值 | 2047 / 0.8 |
| 图类型 | `facts_and_sim_passage_node_unidirectional` |
| 重排模型 | `BAAI/bge-reranker-v2-m3` |
| 重排批大小 / 最大输入长度 | 32 / 512 |

其中，图后端内部检索深度与进入统一融合的候选数属于不同阶段。重排模型固定到版本 `b5160aeac3c6c8fe7beaaaf04c9e0142826b58d1`，以保持路径一致。

### A.6 交叉拟合、诊断与成本核算

每个数据集按固定种子进行五折交叉拟合。在每集 1000 条查询的设置下，每折使用 800 条设计查询、200 条留出查询；五折合并后，每条查询恰好作为测试样本一次。每折的区域边界、特征和物化集合均由该折设计查询重新确定。留出查询的标注仅用于评价，不用于设计决策。

相对预算取 \(b\in\{0,0.1,0.2,0.4,0.6,1.0\}\)。证据检索报告 Evidence Recall@5/10 与 Complete Evidence@5/10；答案评价使用固定读者模型报告 EM/F1，当前正式配置在预算 0.2 下使用前 5 个结果作为阅读上下文。

BM25、Dense 和 Hybrid 对照共享候选深度与重排器。Full Graph 为覆盖完整语料的单一图，保留跨区域事实与同义连接，不能由独立区域图简单拼接替代。分区消融包括仅查询边、仅语义边，以及保持组合分区区域大小分布的随机文档分配；它们用于区分工作负载联系、语义连接和区域规模的作用。

路由诊断分别统计命中任一正确证据区域和命中全部正确证据区域的查询比例。该统计衡量基础入口能否触达目标区域；由于最终图调用还受到物化集合限制，路由覆盖不等于实际图增强覆盖，也不等于最终证据召回。

成本采用三个层面的记录：

- **设计投入**：探测区域的真实构图成本，以及分区、特征计算、收益模型训练和探测检索等设计过程的耗时或调用统计。
- **部署投入**：最终集合中的图索引，记录 LLM 输入与输出 token、嵌入 token、构建耗时、节点与边数、存储量及估算费用。
- **在线投入**：留出查询实际调用区域图和完成检索所需的调用量、耗时与相关 token 开销。

首次构图采用“最终部署图成本 + 全部探测图成本”的保守报告口径。该加总包含被部署复用的探测图的重复计量，因而不等于缓存复用后实际新增的构图量；报告时保留这一说明，并另外列出非图设计耗时。共享基础索引覆盖完整语料，其成本不计入选择性构图预算。质量—成本曲线同时使用实际构建统计，避免将文本规模代理直接当作实测费用。
